# server

Express + TypeScript backend for the AI Chat Bot. Houses HTTP API, WebSocket chat, Bull worker, and all adapters (vector store, object storage, LLM, embeddings).

Scaffolding arrives in Phase 1 — see root [CLAUDE.md](../CLAUDE.md).
